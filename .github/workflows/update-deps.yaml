---
name: Update dependencies

# yamllint disable-line rule:truthy
on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"

permissions: {}

jobs:
  update-deps:
    uses: ./.github/workflows/reusable-update-deps.yaml
    permissions:
      contents: read
    secrets: inherit
